<h1>Showing Reports!</h1>


<p>
  <strong>&lt% some ruby code %&gt</strong>  => this translates to : 'evaluate ruby code'
</p>
<p>
  <strong>&lt%= some ruby code %&gt</strong> => the extra '=' translates to : 'evaluate and display ruby code'
</p>
<p><strong>Author: </strong><%= @report.user.username if @report.user %></p>
<p><strong>Title: </strong><%= @report.title %></p>
<p><strong>Description: </strong><%= @report.description %></p>

<p>
  <% if logged_in? && current_user == @report.user %>
    <%= link_to 'Edit', edit_report_path(@report) %>  |
    <%= link_to 'Delete', report_path(@report), method: :delete, data: { confirm: " Confirm Report deletion"} %>  |
  <% end %>
  <%= link_to 'Back', reports_path%>
</p>
